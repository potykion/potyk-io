# Контент-карточка

Карточка-ссылка **с обложкой**: сверху изображение, снизу текст. В разметке — класс `content-card`. Живёт в общей [сетке карточек](card-grid.md).

## Зачем

Показывать записи и товары с картинкой: обложка читается сразу, подпись не мешает пропорциям карточки.

## Внешний вид

Текст под обложкой по центру. При наведении — лёгкая тень и сдвиг вверх (как у любой карточки в сетке).

Под обложкой:

1. **заголовок** (обязательно);
2. **подзаголовок** (опционально) — второй строкой, серым.

Если подзаголовка нет — остаётся одна строка заголовка. Остальной текст превью в карточке не показывается.

### Заголовок в одну строку

Опциональный режим сетки: заголовок всегда **одна строка**. Если текст длинный — размер шрифта уменьшается, пока название не влезет (без переноса).

Сейчас включено на `/cinema/`; тот же режим можно включить для любой сетки контент-карточек.

### Пропорции

| Ratio | Когда |
|-------|--------|
| **3:4** | по умолчанию (альбомы, сладости, art…) |
| **4:3** | лента `/mu/` с обложкой, Videos |
| **9:16** | постеры фильмов на `/cinema/` |

## Где используется

- `/food/tasting/sweets` — название сладости и производитель; **3:4**;
- `/mu/artists/<slug>` (блок «Albums») — название альбома и год; **3:4**;
- `/mu/artists/<slug>` (блок «Videos») — название ролика и дата; **4:3**;
- лента `/mu/` — записи **с** `cover` в frontmatter; **4:3**;
- `/cinema/` — русское название с годом, английское серым; без обложки — заглушка; **9:16**; заголовок в одну строку (шрифт подгоняется);
- `/reads/` — название книги, подзаголовок и автор серым; без обложки — заглушка; **3:4**; сетка в **4 колонки** (мобилка — **2**); заголовок в одну строку (шрифт подгоняется);
- potyk-art и другие сетки с обложкой — по умолчанию **3:4**.
